apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "node-dan.fullname" . }}-test-connection"
  labels:
    {{- include "node-dan.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "node-dan.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
